export interface CTAButtonProps {
  text: string;
  href: string;
  delay?: number;
  icon?: string;
}

export interface HeroTextProps {
  text: string;
  delay?: number;
}
export interface HeroTitleProps {
  text: string;
}
